---
# tasks file for fedora-software
- name: Install basic command line tools
  become: yes
  ansible.builtin.yum: 
    pkg:
      - bat
      - curl
      - exa
      - gcc
      - gcc-c++
      - git
      - htop
      - kernel-devel
      - make
      - ncdu
      - ripgrep
      - tree
      - tmux
      - zsh
    state: present

- name: Install more software
  become: yes
  ansible.builtin.yum: 
    pkg:
      - alacritty
      - aria2
      - btop
      - detox
      - duf
      - flatpak
      - git-delta
      - handbrake
      - irssi
      - neofetch
      - neovim
      - mpv
      - mupdf
      - oksh
      - ripgrep
      - syncthing
      - tldr
      - torbrowser-launcher
      - yt-dlp
# maybe tmux-powerline
#      - yadm
    state: present

# ffmpeg from rpm fusion?
# yadm https://yadm.io/docs/install#rpm-based-installations

- name: Setup Flathub remote for flatpak
  community.general.flatpak_remote:
    name: flathub
    state: present
    flatpakrepo_url: https://dl.flathub.org/repo/flathub.flatpakrepo
      #    method: user

- name: Add flatpak packages
  community.general.flatpak: name={{item}} state=present #method=user
  with_items:
    - ch.protonmail.protonmail-bridge
    - com.bitwarden.desktop
    - com.github.marktext.marktext
    - com.github.tchx84.Flatseal
    - com.mattermost.Desktop
    - com.spotify.client
    - com.transmissionbt.Transmission
    - io.gitlab.librewolf-community
    - org.libreoffice.LibreOffice
    - org.musicbrainz.Picard
    - org.remmina.Remmina
    

